#include <stdio.h>
#include <iostream>

using namespace std;

int main42()
{
	int a;
	cin >> a;
	cout << (a << 1) << endl;
	return 0;
}
//시프트 연산으로 << 으로 1을 옮기면 2진수에 따라서 비트의 개수가 2배씩  증가 >>으 부호를 사용하면 2의 배수대로 감소한다.

