#include <stdio.h>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>

using namespace std;
int main()
{
	char arr[30];
	cin.getline(arr, 30);
	int len = strlen(arr);
	for (int i = 4; i >= len; i--)
		{
		int num = arr[i] - '0';
		
		for (int i = 0; i <= len; i++)
			{
				
			}
		
		}
}
